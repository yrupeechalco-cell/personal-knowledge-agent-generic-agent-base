# 0.3.5 电脑端知识卡片验收

日期：2026-09-27。安装包对应源码提交 `c6af5d12de5fac9be3673bddf6ad78fbace3e776`。

## 自动测试与构建

- [GitHub Windows 构建 36258473820](https://github.com/yrupeechalco-cell/personal-knowledge-agent-generic-agent-base/actions/runs/36258473820) 全部成功。
- JavaScript / TypeScript 测试 207 项通过，包含 4 项新增卡片交互测试；全工作区类型检查、桌面构建与演示内容排除检查通过。
- Rust 测试 **64 项通过、0 失败、1 忽略**。忽略项为需真实 Codex 授权的已有外部集成测试。
- 新增原生测试覆盖：保留旧索引的迁移、损坏索引中止、资料编号持久化、仅本机范围、共享字段白名单、旧版本保存拒绝、离线标注、原件字节与修改时间不变，以及不把旧卡片任意绑定到多个重命名候选之一。
- GitHub 原生测试输出的 `Cargo.lock` 与提交源码的 SHA-256 一致。
- TypeWords 源码清单、内置资源、独立运行环境启动和新浏览器渲染检查通过；已有手机存储与冲突处理回归通过。后者不代表新账户卡片同步已接入。

## 真实安装版验收

已下载同一个 GitHub 签名安装包并安装到本机，安装程序退出码为 0，程序版本为 **0.3.5**。

| 项目 | 结果 |
| --- | --- |
| 安装包 | `Agent_0.3.5_x64-setup.exe`，44,732,391 字节 |
| SHA-256 | `22d0136e70894201a38edcb1a992ef909c5093cc622fe24e4ad754d292f3be87`，与 GitHub 资产摘要一致 |
| 更新清单 | 公共 `latest.json` 为 0.3.5，签名与下载的 `.exe.sig` 一致，稳定安装地址 HTTP 200 |
| 旧资料迁移 | 合成 v2 索引中的摘要、分类、标签和原文引用保留；新增稳定卡片编号，默认仅本机 |
| 原件保护 | 在真实桌面界面修改卡片后，测试原件 SHA-256 和修改时间均不变 |
| 原件离线 | 暂时移开合成来源目录后可继续保存标注；恢复目录再扫描，标注保留 |
| 重启持久化 | 关闭并重新启动正式 App，卡片编号、摘要和共享范围保持一致 |
| 版本冲突 | 旧卡片版本的保存被原生接口拒绝，较新标注不被覆盖 |
| 共享边界 | 实际预览与原生卡片快照仅含选为可共享的卡片；未出现正文测试标记、引用原文、路径、旧配对记录 |
| 网络接口 | 未授权卡片请求返回 401；旧正文快照、正文写回、附件下载和上传路由全部返回 409 |
| 旧自动同步 | 启动不恢复此前的文件传输服务；重启后再次确认未启动 |
| 迁移回滚保护 | 真实 App 初次加载及重启后，合成旧 `index-v1.json` 的 SHA-256 保持一致 |

原生界面测试使用隔离的合成资料和独立 WebView2 配置目录，方法参考 [Playwright 官方 WebView2 测试说明](https://playwright.dev/docs/webview2)。测试前保存原索引与设置，结束后逐字节恢复，没有上传个人资料或授权信息。第一次验收脚本因“卡片分类”与“按卡片分类查看”的模糊定位中断；改为精确定位后，完整初次启动和重启流程通过。该修正仅涉及验收脚本。

另以浏览器合成界面检查 1440×960 和 1024×720 的布局、草稿保护、共享预览、离线卡片标注、无横向溢出及无页面异常；正式安装版截图也已人工查看。

JSON 共享结构已通过真实原生接口验证；系统原生“另存为”弹窗的人工选址流程未纳入自动脚本。

## 发布与回滚

- [正式发布 app-v0.3.5](https://github.com/yrupeechalco-cell/personal-knowledge-agent-generic-agent-base/releases/tag/app-v0.3.5)。
- 改造前标签：`rollback/desktop-before-knowledge-cards-2026-09-27`，指向 `e8701c9`。
- 新数据保存在 `index-v3.json`；升级前的 `index-v1.json` 保留。具体操作见 [电脑端卡片使用与回滚](DESKTOP_KNOWLEDGE_CARDS.md)。

本轮是电脑端卡片试用。个人账户、手机卡片互通、跨设备关系推荐、标注操作日志与删除传播尚未接入。卡片快照预览和导出不能描述成已经完成的账户同步。
